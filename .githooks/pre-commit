#!/bin/sh
set -eu

echo "Running NOUS OS pre-commit checks..."

git diff --check
PYTHONPATH=src NOUS_OS_HOME="${TMPDIR:-/tmp}/nous-os-precommit" python3 -m unittest discover -s tests -v

echo "NOUS OS pre-commit checks passed."
