# NOUS OS V2 Roadmap

## North Star

NOUS OS V2 is an education and research project for studying a human-AI learning system pattern:

```text
Human intent + Obsidian knowledge sedimentation + verified memory + event mesh + domain runtime + outcome proof + human authority.
```

The goal is not a fully autonomous agent company or a commercialization-first SaaS. The goal is a reusable research and education harness that runs real cognitive work, improves across measured cycles, and preserves human authority at every irreversible boundary.

## Layer Map

| Layer | System | Responsibility |
|-------|--------|----------------|
| Intent / orchestration control plane | Hermes / Aria | Intent parsing, user alignment, specialist routing, operator synthesis, human decision capture |
| Knowledge sedimentation | Obsidian | Human-readable North Stars, playbooks, architecture maps, handoffs, reviews, and durable judgment notes |
| Memory / trust substrate | TrustMem | Episodic memory, knowledge trust, verification, decay, promotion, cross-agent recall |
| Signal / event mesh | Synapse | Event Bus, Blackboard, DAG execution, worker pool, budget routing |
| Domain runtime | First vertical application / research proof: trading-agent | Account/runtime truth, risk and reconciliation gates, outcome ledgers, domain-specific constraints used to study human-AI boundaries |
| Interaction surfaces | Dashboard, Telegram / ARIA, CLI | Human-readable review, briefing, and operator workflow |

## Current Phase

Current phase:

```text
V2 product-hardening / first-vertical proof consolidation
```

NOUS OS already has a local/public demo, heartbeat dashboard, benchmark frame, CI, and Pages deployment. The current focus is making the first vertical proof loop repeatable and grounded in real evidence before expanding the product surface.

### Status snapshot — 2026-05-16

Shipped this cycle (read-only, boundary-respecting):

- **Domain evaluator runtime contract** — `examples/runtime/domain_evaluator.py` exposes a runtime-checkable `DomainEvaluator` Protocol plus `validate_cls_components()` shape validator. Future second-vertical adapters inherit the same guarantees instead of reinventing them.
- **TradingEvaluator (`examples/runtime/trading_evaluator.py`)** — first real `DomainEvaluator` implementation, mapping all six CLS v2 components from existing trading-agent proof artifacts (`market_proof/baseline_comparisons.jsonl`, `forecast_ledger_summary.json`, `promotion_reviews/proof_packs/*.json`). Pure read; no broker, order, risk, or live-state effects.
- **`trading_vertical` demo mode wired to real evidence** — when trading-agent artifacts are present, CLS v2 is routed through `TradingEvaluator`; otherwise the dashboard explicitly labels itself `synthetic_demo_fallback` with a concrete reason. The `evidence_source` field is rendered in the public dashboard so a viewer can tell at a glance whether a benchmark is grounded.
- **Release gate allowlist** — `scripts/check_cross_repo_release_gate.py` now distinguishes true blockers from autogenerated noise (`knowledge/distilled/`, `knowledge/ai-infra/promoted-`, `agent-bus/`, `data/episodes/`, `research/metrics/`). The gate has a usable green state again.
- **Documentation reproducibility smoke** — `tests/test_documentation_reproducibility.py` confirms every `python3 <path>` command documented in `README.md` and `docs/getting-started.md` resolves to an existing target and that `scripts/run_nous_heartbeat.py` runs end-to-end without external sibling repos.
- **Harness inventory** — `docs/harness/HARNESS_INVENTORY.json` is the machine-readable surface map (16 surfaces, validator-enforced, contract-tested) for any agent picking up NOUS OS work.

In flight or owner-team work:

- `trading-agent`-side ReviewedExperiment ledger population (criterion 1).
- `trading-agent`-side capital-boundary documentation tested end-to-end (criterion 6 deeper).
- Live `dirty=False` for `trading-agent` once owner-team WIP entries are committed or intentionally accepted.

See [second-vertical-entry-criteria.md](./second-vertical-entry-criteria.md) for the live gate; criteria 2, 3, 4, 5 are advanced by the work above.

## Next Phase Triggers

The next phase opens once the first vertical proof loop is unambiguously closed:

1. Trading-agent ReviewedExperiment ledger is populated with at least three reviewed outcomes that flow through `TradingEvaluator` into a tracked dashboard snapshot.
2. Cross-repo release gate is green (`ok=true`) without temporary exceptions.
3. A first external viewer (parent, teacher, researcher, or student) can reproduce the demo and explain the boundary lessons without coaching.

Until those three are met, work stays in this phase: hardening the proof loop, the evaluator, the gates, the demo, and the review protocol. Expansion is explicitly out of scope.

## First Vertical Application / Research Proof

Trading Brain / `trading-agent` remains the first vertical application for NOUS OS. It is not the commercialization endpoint of NOUS OS; it is the education/research traction case. The point is to use a real, high-constraint, measurable domain to study how humans and AI collaborate, where boundaries should sit, how judgment is preserved, and how outcomes feed the next learning cycle.

Trading Brain / `trading-agent` is a good first vertical because it has high stakes, measurable outcomes, strong feedback, and hard human authority boundaries.

The generic loop:

```text
intent -> Obsidian knowledge sedimentation -> verified memory -> event routing -> domain runtime -> outcome -> verified learning -> next intent
```

The Trading Brain loop:

```text
market/user intent -> Obsidian investment notes/playbooks -> verified memory -> research/risk agents -> trading-agent runtime -> measured outcome -> scoreboard/lesson -> next decision
```

## Explicit Deferrals

These are not current-phase work:

- second vertical expansion
- new trading strategy
- new broker integration
- new top-level dashboard tab
- fully autonomous trading company
- multi-tenant SaaS launch

Second vertical work is gated by [second-vertical-entry-criteria.md](./second-vertical-entry-criteria.md).

## Boundaries

NOUS OS may score, route, recall, validate, summarize, and generate review packets. It must not authorize capital actions, bypass broker/risk/reconciliation gates, treat Obsidian as live state, or mistake synthetic demo quality for production evaluation.

See [domain-evaluator-interface.md](./domain-evaluator-interface.md) for the production evaluator boundary and [harness/README.md](./harness/README.md) for the broader harness boundary map.
